{
    "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
        "secretValue": {
            "type": "securestring"
        }
    },
    "resources": [],
    "outputs": {
        "secretValueOutput": {
            "type": "string",
            "value": "[parameters('secretValue')]",
            "metadata": {
                "note": "this is going to show the secret value in deployment output (so it's no longer secret) - for demo purposes only."
            }
        }
    }
}